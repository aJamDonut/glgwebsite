# glgwebsite

## Local preview

Run a tiny local dev server with Node.js:

```bash
npm run dev
```

Then open http://127.0.0.1:5173 in your browser.

You can change the port (PowerShell) with:

```bash
$env:PORT=3000; npm run dev
```
